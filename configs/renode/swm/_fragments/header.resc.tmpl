####################################################################
# SWM Mixed-SoC Pair — Generated Renode Script
# File: configs/renode/swm/generated/multinode_@@A_TARGET@@_@@A_ROLE@@__@@B_TARGET@@_@@B_ROLE@@.resc
#
# Generated by scripts/gen-multinode-resc.sh (Phase 6.3.1 of
# tmp/stm32_renode_parity_plan.md).  Do not hand-edit — re-generate
# instead.  Source template fragments live in:
#   configs/renode/swm/_fragments/header.resc.tmpl
#   configs/renode/swm/_fragments/endpoint_<target>.resc.tmpl
#
# Pair: A = @@A_TARGET@@ / @@A_ROLE@@
#       B = @@B_TARGET@@ / @@B_ROLE@@
####################################################################

# Resolve relative paths via the workspace root (PWD).
python "import System.IO, System; System.IO.Directory.SetCurrentDirectory(System.Environment.GetEnvironmentVariable('PWD'))"

# ── Compile peripheral C# models (once, before creating machines) ─
i $ORIGIN/../../shared/peripherals/lora/lr1121_subghz.cs
i $ORIGIN/../../shared/peripherals/lora/lr1121_radio.cs
i $ORIGIN/../../shared/peripherals/console/usb_serial_jtag.cs
i $ORIGIN/../../shared/peripherals/lora/lora.cs

# ── Wireless medium ──────────────────────────────────────────────
# Single shared medium for both endpoints.  Range = 200 m matches
# Phase 4.3's swm_multinode_mixed.resc default.
emulation CreateIEEE802_15_4Medium "wireless"
wireless SetRangeWirelessFunction 200
